package pl.ipebk.solver

/**
 * TODO: Generic description. Replace with real one.
 */
class SetGameSolver(val numberOfFeatures: Int, val numberOfFeatureVariants: Int) {
    fun findSolution(cards: List<Card>) : List<Card> {
        val solution = ArrayList<Card>()
        return solution
    }
}